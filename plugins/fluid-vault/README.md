# fluid-vault

Skills for reviewing **Instadapp Fluid** vault / liquidity patterns (bug bounty & audit support).

| Skill | Description |
|-------|-------------|
| `fluid-liquidate-muldiv-clamp` | Inverted integer mul/div when clamping liquidated collateral |

Expand this plugin as Fluid-specific patterns accumulate. May be split to a dedicated git submodule later.
